# AGENTS.md

1. 每个任务超过一段时间没有输出结果要自动暂停，询问我的意见再继续
2. 下载网页内容优先用 PowerShell Invoke-WebRequest，不用 Python urllib
3. 不要同时启动多个下载进程，一次只做一个
4. 命令超过10秒没输出就中断换方案，不重复重试同一方法
5. 浏览器工作只在需要交互（点击、翻页）时才用，纯抓取走 Invoke-WebRequest
6. 安全规则：永远不要输出、回显、打印、记录、缓存或提交任何密码、API Key、Token、私钥、密钥文件或其他机密信息。所有敏感内容必须自动脱敏，禁止出现在任何回复、日志、代码、提交记录或生成文件中。
7. 如果 thinking / waiting 超过 1 分半（90 秒），必须主动中断并告诉我当前运行是否正常，不要让我空等。
